Users
user_id     username    password_hash

Leagues
league_id   name        admin_id        num_participants    join_code

Users_Leagues
league_id   user_id

Teams
user_id     league_id    name       fighter_name  //add slots for each division


Fighter_pool  """universal"""
id          name        weight_class     ranking       last_updated


League_Draft
id  |   league_id |  current_round  |  current_pick_user_id  |  total_picks | draft_order JSON  |  status ENUM('not_started', 'in_progress', 'complete'),

Draft_Picks
id |   league_id | user_id |  fighter | weight_class | round





CREATE TABLE Fighter_pool (
    id INT PRIMARY KEY AUTO_INCREMENT,
    name VARCHAR(255) NOT NULL,
    weight_class VARCHAR(50) NOT NULL,
    ranking INT,                -- 1 to 15
    last_updated DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
);



league_draft (
  id INT PRIMARY KEY,
  league_id INT,
  current_round INT,
  current_pick_user_id INT,
  total_picks INT,
  draft_order JSON,
  status ENUM('not_started', 'in_progress', 'complete')
)

draft_picks (
  id INT PRIMARY KEY,
  league_id INT,
  user_id INT,
  fighter varchar,
  weight_class VARCHAR,
  round INT
)